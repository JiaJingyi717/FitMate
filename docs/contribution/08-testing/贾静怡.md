# 软件测试贡献说明

姓名：贾静怡  
学号：2323030103  
角色：后端  
日期：2026-04-26  

## 完成的测试工作

### 测试文件

- tests/test_auth.py
- tests/test_plan.py
- tests/test_users.py
- tests/test_analytics.py
- tests/test_articles.py
- tests/test_coaches.py
- tests/test_ai_routes.py
- tests/test_ai_service.py
- tests/test_user_service.py
- tests/conftest.py

### 测试清单

- [x] 接口与服务层测试（共 79 个用例，pytest collected 79）
- [x] 边界 / 异常分支测试（含超时、连接异常、参数缺失、资源不存在等）
- [x] Mock 使用

### 覆盖率

- 后端总体覆盖率：80%（`pytest --cov=. --cov-report=term-missing`）
- 关键模块：
  - `routes/auth_routes.py`: 96%
  - `routes/plan_routes.py`: 93%
  - `services/plan_service.py`: 79%
  - `routes/ai_routes.py`: 75%
  - `services/user_service.py`: 100%

### AI 辅助

- 工具：Cursor
- Prompt：按后端接口与服务逻辑补齐测试、强化异常分支、修复失败用例并提升覆盖率（80%）
- 新增测试文件：3 个（`test_ai_routes.py`、`test_ai_service.py`、`test_user_service.py`）

## PR 链接

- PR \#20:  https://github.com/JiaJingyi717/FitMate/pull/20

## 遇到问题与解决

1. 认证相关测试失败（注册参数与接口契约不一致）→ 调整 `conftest` 与 `test_auth` 的请求体，补充 `name` 字段并修正登录取 token 逻辑。  
2. AI 相关模块覆盖率偏低（`ai_routes`、`ai_service`）→ 新增 AI 路由与服务层测试，覆盖成功路径与超时/连接异常/参数校验等分支。  
3. `test_user_service.py` 在编辑过程中出现重复内容，导致覆盖率统计失真 → 重建文件为单份内容后重新回归，覆盖率恢复并稳定到 80%。  

## 心得体会

本次后端测试先修复存量失败用例，再集中补齐高收益模块（认证、计划、用户、AI 路由/服务层）的关键分支。通过“增量补测 + 每轮回归覆盖率”方式，后端覆盖率由 64% 提升到 80%，并形成了可持续维护的测试基线。