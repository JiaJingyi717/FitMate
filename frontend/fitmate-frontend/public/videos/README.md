# 本地健身视频（可选）

将 `.mp4` 文件放在此目录，例如 `shoulder-training.mp4`，然后在数据库 `articles.video_url` 或管理后台中填写：

```
/videos/shoulder-training.mp4
```

Vite 会通过 `http://localhost:5173/videos/...` 直接提供静态文件。

也可使用外链 MP4、B 站（`https://www.bilibili.com/video/BV...`）或 YouTube 链接。
